# Set prefix key
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Enable mouse
set -g mouse on

# Set window options
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

# Recommended settings for Vim
set -g escape-time 10
set -g focus-events on
